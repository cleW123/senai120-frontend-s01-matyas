//Escreva um programa que peça para o usuário digitar uma letra e verifique se ela é uma vogal ou uma consoante.
 let letra = "a"

let vogal = "a" || "e" || "i" || "o";

 if(letra == vogal)(
    console.log("isso e uma vogal")
 )
 else(
    console.log("isso e uma consoante")
 )